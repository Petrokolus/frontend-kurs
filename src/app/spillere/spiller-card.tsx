type props = {
  navn: string;
  avdeling: string;
  // Legg til flere props basert på datamodellen 
};

export default function SpillerCard({ navn, avdeling }: props) {
  return (
    <div className="flex items-center space-x-4 rounded-lg border p-4">

    </div>
  );
}